version: "3.9"
services:
  postgres:
    image: postgres:latest
    container_name: third_lesson
    environment:
      POSTGRES_DB: "third_lesson"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "root"
    ports:
      - "5432:5432"